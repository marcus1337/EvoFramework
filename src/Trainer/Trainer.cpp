#include "Trainer.h"

Trainer::Trainer(int _numAIs, int _numInputNodes, int _numOutputNodes, std::string _environmentScript) : 
	numAIs(_numAIs), numInputNodes(_numInputNodes), numOutputNodes(_numOutputNodes) {

}